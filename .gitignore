# ═══════════════════════════════════════════════════════════════════
# .NET / ASP.NET Core — Portfolio_game_dev
# ═══════════════════════════════════════════════════════════════════

# ── 1. Артефакты сборки ────────────────────────────────────────────
[Bb]in/
[Oo]bj/
[Oo]ut/
[Dd]ebug/
[Rr]elease/
x64/
x86/
artifacts/
*.dll
*.pdb
*.exe
# Исключения: .dll в инструментах, если появятся
!tools/**/*.dll

# ── 2. Пользовательские настройки IDE ──────────────────────────────
.vs/
.idea/
.vscode/
*.suo
*.user
*.userosscache
*.sln.docstates
*.csproj.user
.ionide/

# ── 3. NuGet ───────────────────────────────────────────────────────
*.nupkg
*.snupkg
**/[Pp]ackages/*
!**/[Pp]ackages/build/
project.lock.json
project.fragment.lock.json
*.nuget.props
*.nuget.targets

# ── 4. Логи ────────────────────────────────────────────────────────
[Ll]og/
[Ll]ogs/
logs/
*.log

# ── 5. SQLite БД (локальная разработка) ────────────────────────────
# Основная БД проекта — создаётся миграцией + DbSeeder, не коммитится
portfolio.db
portfolio.db-shm
portfolio.db-wal

# Общее правило для .db/.sqlite — на случай тестовых/временных БД
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.test.db

# ── 6. Секреты и локальные настройки ───────────────────────────────
# Локальные файлы с паролями, токенами — не для публичного репо
appsettings.Local.json
appsettings.*.Local.json
secrets.json

# .env для Docker/деплоя
.env
.env.local
.env.*.local

# (Опционально) appsettings.Development.json — раскомментируй,
# если держишь в нём реальный пароль админа:
# appsettings.Development.json

# ── 7. Пользовательские загрузки ───────────────────────────────────
# Картинки/видео, загруженные через админку. Не коммитим.
wwwroot/uploads/*
!wwwroot/uploads/.gitkeep

# ── 8. Публикация / деплой ─────────────────────────────────────────
publish/
*.publishsettings
*.pubxml
*.pubxml.user
PublishProfiles/
.azure/

# ── 9. Файлы Visual Studio (мусор) ─────────────────────────────────
*.tmp
*.tmp_proj
*.cache
*.pidb
*.svclog
*.scc
*.pch
*.sbr
*.tlb
*.tlh
*.iobj
*.ipdb
MigrationBackup/

# ── 10. Результаты тестов и покрытия ───────────────────────────────
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
*.coverage
*.coveragexml
coverage*.json
coverage*.xml
coverage*.info
BenchmarkDotNet.Artifacts/
nunit-*.xml
TestResult.xml

# ── 11. Личные заметки и черновики ─────────────────────────────────
# Не для публичного репо
/Notes/
/TODO.md
/scratch/

# ── 12. Операционные системы ───────────────────────────────────────
# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
desktop.ini

# ── 13. Редакторы ──────────────────────────────────────────────────
*~
*.swp
*.swo
*.bak
*.orig
*.rej