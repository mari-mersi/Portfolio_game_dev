@{
    ViewData["Title"] = "Проекты";
}

<div class="max-w-5xl">
    <div class="flex items-center justify-between mb-6">
        <div>
            <h1 class="text-2xl font-bold text-slate-900">Проекты</h1>
            <p class="text-sm text-slate-500 mt-1">Управление портфолио проектов.</p>
        </div>
        <button disabled
                class="px-4 py-2 rounded-lg bg-slate-300 text-white text-sm font-medium cursor-not-allowed">
            + Добавить проект
        </button>
    </div>

    <div class="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center">
        <p class="text-slate-500 mb-2">CRUD проектов появится в Дне 9.</p>
        <p class="text-xs text-slate-400">
            Пока проекты редактируются через <code>DbSeeder</code> и пересоздание БД.
        </p>
    </div>
</div>